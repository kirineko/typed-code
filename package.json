{
  "name": "typed-code",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/sdk",
    "packages/cli",
    "packages/server-darwin-arm64"
  ],
  "scripts": {
    "build": "npm run build -w @typed-code/sdk && npm run build -w @typed-code/cli",
    "build:companion": "uv run --with pyinstaller==6.22.0 python packaging/build_companion.py",
    "pack:release": "uv run python packaging/package_release.py",
    "check": "npm run build && npm run check -w @typed-code/sdk && npm run check -w @typed-code/cli",
    "dev:link": "npm run build && npm link -w @typed-code/cli",
    "test:unit": "npm run build -w @typed-code/sdk && npm run test -w @typed-code/sdk && npm run test -w @typed-code/cli"
  },
  "engines": {
    "node": ">=22"
  }
}
